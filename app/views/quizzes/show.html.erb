<% provide(:title, @quiz.name) %>
<p><%= @quiz.name %></p>

<h4>Easy</h4>

<% @easy.each do |question| %>
	<p><%= question.text %>    <%= question.maxValue %></p>
	<ul>
		<% question.answers.each do |answer| %>
			<li><%= answer.text %>      <%= answer.value %> </li>
		<% end %>
	</ul>
<% end %>

<h4>Medium</h4>

<% @med.each do |question| %>
	<p><%= question.text %>    <%= question.maxValue %></p>
	<ul>
		<% question.answers.each do |answer| %>
			<li><%= answer.text %>      <%= answer.value %> </li>
		<% end %>
	</ul>
<% end %>

<h4>Hard</h4>

<% @hard.each do |question| %>
	<p><%= question.text %>    <%= question.maxValue %></p>
	<ul>
		<% question.answers.each do |answer| %>
			<li><%= answer.text %>      <%= answer.value %> </li>
		<% end %>
	</ul>
<% end %>

	<%= link_to "Edit Quiz", edit_quiz_path %>