<% provide(:title, 'Sign Up')%>
<% content_for :pageBar do %>
	<%= link_to "Users", users_path %> > <%= link_to "Create User", new_user_path %>
<% end %>
<h1>Sign Up</h1>
<%= form_for(@user) do |f| %>

	<%= render 'shared/form', f: f %>

	<p><%= f.submit "Create Account" %></p>

<% end %>